@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{/*
    @model IEnumerable<MvcSoruCevap.Models.Answer>
    @{var question = ViewBag.Question as MvcSoruCevap.Models.Question;}

    <h1>@question.Title</h1>
            <p>@question.Content</p>

    <a href="/Answer/Create?questionId=@question.QuestionId" class="btn btn-primary">Yeni Cevap Ekle</a>

        <table class="table">
            <thead>
                <tr>
                <th>ID</th>
                <th>Cevap</th>
                <th>Kullanıcı</th>
                <th>Oluşturulma Tarihi</th>
            <th>İşlemler</th>
        </tr>
        </thead>
            <tbody>
            @foreach (var answer in Model)
                {
                    <tr>
                    <td>@answer.AnswerId</td>
                    <td>@answer.Content</td>
                    <td>@answer.User.Username</td>
                    <td>@answer.CreatedAt</td>
                        <td>
                        <a href="/Answer/Edit/@answer.AnswerId" class="btn btn-warning">Düzenle</a>
                        <a href="/Answer/Delete/@answer.AnswerId" class="btn btn-danger">Sil</a>
                </td>
            </tr>
        }
    </tbody>
            </table>
    */
}
